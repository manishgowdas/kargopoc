name: Kargo PoC on kind
on:
  workflow_dispatch:
jobs:
  kargo-poc:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Install kubectl
        uses: azure/setup-kubectl@v4
      - name: Install Helm
        uses: azure/setup-helm@v4
      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/latest/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind
      - name: Create kind cluster
        run: |
          kind create cluster --name kargo-demo
      - name: Verify cluster
        run: kubectl get nodes
      # -----------------------------
      # Install Argo CD (fixed)
      # -----------------------------
      - name: Install Argo CD (core only - FIXED)
        run: |
          kubectl create namespace argocd || true
          kubectl apply -n argocd --server-side \
            -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/core-install.yaml
      - name: Wait for Argo CD
        run: |
          sleep 30
          kubectl rollout status deployment/argocd-redis -n argocd --timeout=300s
          kubectl rollout status deployment/argocd-repo-server -n argocd --timeout=300s
          kubectl rollout status deployment/argocd-applicationset-controller -n argocd --timeout=300s
          kubectl rollout status statefulset/argocd-application-controller -n argocd --timeout=300s
      - name: Verify Argo CD
        run: kubectl get pods -n argocd
      # -----------------------------
      # Install Kargo
      # -----------------------------
      - name: Install Kargo
        run: |
          kubectl create namespace kargo || true
          helm repo add kargo https://charts.kargo.akuity.io
          helm repo update
          helm install kargo kargo/kargo -n kargo
      - name: Wait for Kargo
        run: |
          kubectl wait --for=condition=Ready pods --all -n kargo --timeout=300s
      - name: Verify Kargo
        run: kubectl get pods -n kargo
      # -----------------------------
      # Final check
      # -----------------------------
      - name: Show all resources
        run: kubectl get all -A
